# 仅启动 Redis 容器，供 fullstack 二进制连接（localhost:6379）。
# 如果你想用 PostgreSQL，可在此处自行追加 postgres 服务。

services:
  redis:
    image: redis:7-alpine
    container_name: dujiao-redis
    restart: unless-stopped
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - ./redis-data:/data
